<p id="notice"><%= notice %></p>

<h1>Reviews</h1>

<table>
  <thead>
    <tr>
      <th>Titre</th>
      <th>Contenu</th>
      <th>Produit</th>
      <th>Utilisateur</th>
      <th>Note</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reviews.each do |review| %>
      <tr>
        <td><%= review.title %></td>
        <td><%= review.content %></td>
        <td><%= review.item %></td>
        <td><%= review.user %></td>
        <td><%= review.rating %></td>
        <td><%= link_to 'Détail', review %></td>
        <td><%= link_to 'Modifier', edit_review_path(review) %></td>
        <td><%= link_to 'Supprimer', review, method: :delete, data: { confirm: 'Etes-vous sur ?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvel avis', new_review_path %>
