<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
  	<%= link_to "×", "javascript:void(0)",class: "close", :data => {:dismiss => "alert"} %>
    <ul>
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>